let a = [1, 2, 3, 4, 5];
for (let i = 0; i < a.length; i++) {
  setTimeout(() => {
    console.log(i);
  }, 3000);
}

// for (var i = 0; i <= 3; i++) {

//     setTimeout(()=>console.log(i),1000)

// }

// What is currying in JavaScript?

// What is recursion in a programming language?

// Implement a function that returns an updated array with r right rotations on an array of integers a . Given the following array: [2,3,4,5,7]

// Write a function that performs binary search on a sorted array.

// what is event delegation in javascript?

// what is event bubbling & event capturing?

// How does garbage collection work in Javascript?

// what is the diffrence between weakmap & map in javscript?
